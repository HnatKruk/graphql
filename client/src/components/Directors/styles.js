export const styles = theme => ({
  wrapper: {
    position: 'relative',
    minHeight: 'calc(100vh - 24px * 2 - 72px)',
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'flex-end',
  },
  fab: {
    position: 'absolute',
    bottom: '24px',
    right: '24px',
  },
});
